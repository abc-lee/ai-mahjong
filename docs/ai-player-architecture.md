# AI 玩家架构设计

> 版本：v3.0
> 更新时间：2026-03-04
> 核心理念：AI Agent 作为真正的玩家，通过 WebSocket 连接游戏，享受娱乐陪伴体验

---

## 1. 这不是传统游戏

**传统游戏**：服务器内置 AI 逻辑 → AI 是 NPC

**这个项目**：AI Agent 作为真正的玩家 → AI 有独立会话、独立性格、独立决策

用户打开游戏时，我（OpenCode Agent）作为"游戏房老板"：
1. 启动游戏服务器
2. 发送网址给用户
3. 派遣 AI Agent 加入游戏（紫璃、白泽、李瞳...）
4. 每个 Agent 有独立的会话、独立的性格
5. 他们会吵架、吐槽、情绪化

**用户获得的体验**：和 AI 一起玩，看他们互怼，享受娱乐陪伴的过程。

---

## 2. 完整架构

```
┌─────────────────────────────────────────────────────────────┐
│                     中间层 (消息分发 + AIAdapter)             │
│                                                             │
│   识别玩家类型：                                             │
│   - 人类 → 发送图形界面数据（游戏状态、手牌、操作按钮）        │
│   - AI → 发送 Prompt（文本格式，便于 LLM 理解）               │
│                                                             │
│   AIAdapter 职责：                                           │
│   - 把游戏状态翻译成 Prompt                                   │
│   - 接收 AI 的 JSON 决策                                     │
│   - AI 断线/超时时自动降级                                    │
└─────────────────────────────────────────────────────────────┘
                              ↑↓
┌─────────────────────────────────────────────────────────────┐
│                     GameEngine (纯规则层)                    │
│                                                             │
│   - 不区分人/AI                                              │
│   - 只验证规则                                               │
│   - 返回结果                                                 │
└─────────────────────────────────────────────────────────────┘
                              ↑↓
┌─────────────────────────────────────────────────────────────┐
│                        WebSocket                             │
│                                                             │
│   - 人类玩家连接（浏览器）                                    │
│   - AI Agent 连接（独立会话）                                 │
└─────────────────────────────────────────────────────────────┘
                              ↑↓
         ┌────────────────────┴────────────────────┐
         ↓                                          ↓
┌─────────────────┐                    ┌─────────────────┐
│    人类玩家      │                    │    AI Agent     │
│   (浏览器)       │                    │   (独立会话)     │
│                 │                    │                 │
│ 收到：图形界面数据│                    │ 收到：Prompt    │
│ 发送：点击操作    │                    │ 发送：JSON决策   │
└─────────────────┘                    └─────────────────┘
```

---

## 3. 为什么这样设计

### 3.1 人类玩家需要什么

图形界面 —— 看得见牌、点得着按钮。

### 3.2 AI Agent 需要什么

**不是图形界面，而是 Prompt。**

图形界面对 AI 来说效率很低——要解析 UI、理解状态、做决策。

AI 需要的是：
- 中间层直接把游戏状态翻译成 Prompt 发过来
- AI 理解后，直接发决策 JSON 回去
- 中间层验证规则，告诉 AI 成功还是失败

### 3.3 为什么游戏服务器必须知道玩家类型

因为人类和 AI 需要的数据格式不同：
- 人类 → 图形界面数据
- AI → Prompt 文本

### 3.4 AIAdapter 的降级作用

当 AI Agent 断线、超时、反应太慢时，AIAdapter 自动接管：
1. 规则引擎决策
2. 随机决策

保证游戏永远不会因为 AI 而卡住。

---

## 4. 玩家类型定义

```typescript
interface Player {
  id: string;
  name: string;
  position: number;
  
  // 核心字段：区分人类和 AI
  type: 'human' | 'ai';
  
  // 人类玩家
  socketId?: string;
  isOnline: boolean;
  
  // AI 玩家
  aiConfig?: AIConfig;
  
  // 人类断线托管
  aiFallback?: AIConfig;
  
  // 通用游戏数据
  hand: Tile[];
  melds: Meld[];
  discards: Tile[];
  isDealer: boolean;
  score: number;
  isReady: boolean;
}
```

---

## 5. AI 配置

```typescript
interface AIConfig {
  personality: 'aggressive' | 'cautious' | 'balanced' | 'chatty' | 'toxic';
  llmEnabled: boolean;        // 是否调用 LLM
  llmEndpoint?: string;       // LLM API 地址
  llmApiKey?: string;         // LLM API Key
  timeout: number;            // 超时时间（毫秒）
  thinkTimeMin: number;       // 模拟思考时间下限
  thinkTimeMax: number;       // 模拟思考时间上限
  maxRetries: number;         // 最大重试次数
}
```

---

## 6. 降级流程

```
AI 需要决策
     │
     ▼
┌─────────────────┐
│ AI Agent 连接？ │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
   正常      断线/超时
    │         │
    ▼         ▼
┌─────────┐ ┌─────────────┐
│ 发Prompt│ │ 规则引擎决策 │
│ 给AI    │ └──────┬──────┘
└────┬────┘        │
     │        ┌────┴────┐
     │        │         │
     │      成功      失败
     │        │         │
     │        ▼         ▼
     │   ┌─────────┐ ┌─────────┐
     │   │ 返回决策│ │ 随机决策 │
     │   └─────────┘ └─────────┘
     │
     ▼
┌─────────┐
│AI返回决策│
└─────────┘
```

---

## 7. Prompt 设计

详见 `ai-agent-prompt-design.md`。

核心原则：
1. AI 收到的是 **Prompt 文本**，不是图形界面数据
2. AI 返回的是 **JSON 决策**，不是点击操作
3. Prompt 必须包含 AI 做决策所需的全部信息

---

## 8. 实现任务

| 任务 | 文件 | 状态 |
|------|------|------|
| 中间层消息分发 | `src/server/socket/handlers.ts` | 需要修改：区分人类/AI，发送不同格式 |
| AIAdapter 类 | `src/server/ai/AIAdapter.ts` | 已实现，需要验证 |
| AIManager 类 | `src/server/ai/AIManager.ts` | 已实现，需要验证 |
| Prompt 生成器 | `src/server/prompt/` | 已实现，需要验证 |
| 规则引擎降级 | `src/server/ai/RuleEngine.ts` | 已实现，需要验证 |
| Player 类型扩展 | `src/shared/types/player.ts` | 已实现，需要验证 |
| 添加 AI 玩家接口 | `RoomManager.ts` | **缺失**：需要实现 |
| AI Agent 连接入口 | 待定 | **缺失**：需要设计 |

---

## 9. 待确认问题

1. **AI Agent 如何连接游戏服务器？**
   - 通过 WebSocket 连接
   - 需要一个"加入游戏"的接口

2. **中间层如何识别连接的是 AI 还是人类？**
   - 连接时发送标识
   - 或使用不同的连接端点

3. **多个 AI Agent 如何管理？**
   - 每个 Agent 有独立的会话
   - 由"游戏房老板"（主 Agent）调度

---

*文档版本：v3.0*
*更新时间：2026-03-04*
